@using MySqlConnector
@model ProductionManagementSystem.Core.Models.AltiumDB.TableColumn

<tr id="Tr@(Model.DatabaseOrder)">
  <td class="Ids">
          @Model.DatabaseOrder
      </td>
  <td >
    <div class="bx--text-input__field-wrapper">
      <input id="text-input-1-@Model.DatabaseOrder" type="text" class="bx--text-input bx--text-input--light bx--text-input--sm" placeholder="Placeholder text" name="TableColumns[@(Model.DatabaseOrder - 1)].ColumnName" value="@Model.ColumnName">
    </div>
  </td>
  <td >
    <div class="bx--form-item bx--checkbox-wrapper">
      <input id="bx--checkbox-new-@(Model.DatabaseOrder)" class="bx--checkbox" type="checkbox" value="true" checked="@Model.Display"
             name="TableColumns[@(Model.DatabaseOrder - 1)].Display">
      <label for="bx--checkbox-new-@(Model.DatabaseOrder)" class="bx--checkbox-label"></label>
    </div>
  </td>
  <td>
    <button class="bx--btn bx--btn--ghost bx--btn--sm" type="button" onclick="removeTableColumn(@(Model.DatabaseOrder + 1))">
      <svg width="32" height="32">       
        <image xlink:href="/carbonUIicons/close.svg"  width="32" height="32"/>    
      </svg>
    </button>
  </td>
  <input type="hidden" name="TableColumns[@(Model.DatabaseOrder - 1)].DatabaseOrder" value="@Model.DatabaseOrder"/>
  <input type="hidden" name="TableColumns[@(Model.DatabaseOrder - 1)].Id" value="@Model.Id"/>
</tr>